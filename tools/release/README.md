# Maintainer release tools

These scripts are maintainer release tooling, not product runtime code or
unittest modules. Run them only from a prepared release workspace after setting
the required `EFFORT510_*` environment variables; use each command's `--help`
or shell usage message for its exact inputs.
